# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Een optionele dark mode met een schakelaar in de sidebar — licht, donker of systeem — onthouden in `localStorage`, zonder witte flits bij het laden.

**Architecture:** De app leest al zijn kleuren uit CSS-variabelen, dus het thema kantelt met één `.dark`-blok dat diezelfde tokens overschrijft. Tailwind 4 laat `dark:` standaard naar `prefers-color-scheme` kijken; één `@custom-variant`-regel wijst hem naar de klasse `dark` op `<html>`. Een klein inline script zet die klasse vóór het schilderen. De enige echte logica — opgeslagen keuze plus systeemvoorkeur wordt een toegepast thema — staat als pure functie in `src/lib/theme.ts` en wordt daar getest.

**Tech Stack:** Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS 4.2.4, lucide-react, vitest.

## Global Constraints

- **Node 20 is vereist.** De standaard-node in deze shell is v16 en de toolchain crasht daarop. Prefix élk npm- of npx-commando met `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH"`.
- **Raak de database niet aan.** Dit traject wijzigt het datamodel niet. Draai geen enkel `prisma`-commando; er is een live productiedatabase bereikbaar vanuit deze checkout.
- **Dit is niet de Next.js die je kent.** Lees `node_modules/next/dist/docs/` voordat je aannames doet over de App Router — met name over waar een `<script>` in de root layout hoort. (`AGENTS.md` is een echte, door het team gecommitte projectafspraak.)
- **Geen nieuwe dependencies.** Met name geen `next-themes`: wat het doet is hier vijftien regels.
- **Alle zichtbare tekst is Nederlands.** De drie standen heten exact `Licht`, `Donker` en `Systeem`.
- **Tailwind is versie 4.2.4** en heeft géén `tailwind.config.js`. Alles staat in `src/app/globals.css`. In v4 luistert `dark:` standaard naar `prefers-color-scheme`; zonder de `@custom-variant`-regel uit Task 2 doet elke `dark:`-utility dus het verkeerde.
- **De opslagsleutel is `"theme"`** en de waarden zijn exact `"light"`, `"dark"` en `"system"`.
- Testcommando: `npm test`. Baseline: **23 bestanden, 248 tests groen.**
- Lint: `npm run lint`. **De baseline is niet schoon:** 323 errors en 20 warnings, vrijwel allemaal `@typescript-eslint/no-explicit-any`. De gate is *geen nieuwe soorten lint-fouten*.
- **`npx tsc --noEmit` meldt 0 fouten over de hele repo en moet dat blijven doen.**
- `npm run build` kan niet draaien zonder `.env`; gebruik `tsc` als vangnet.
- Testconventie: uitsluitend pure functies in `src/lib/*.test.ts` met vitest. Er bestaan geen component- of CSS-tests en die worden hier niet geïntroduceerd.

---

## File Structure

**Nieuw:**

| Bestand | Wat |
|---|---|
| `src/lib/theme.ts` | `THEME_STORAGE_KEY`, types, `resolveTheme`. |
| `src/lib/theme.test.ts` | Tests daarvoor. |
| `src/components/layout/theme-toggle.tsx` | De schakelaar met drie standen. |

**Gewijzigd:**

| Bestand | Wat |
|---|---|
| `src/app/globals.css` | `@custom-variant` en het `.dark`-blok. |
| `src/app/layout.tsx` | Het inline script en `suppressHydrationWarning`. |
| `src/components/layout/sidebar.tsx` | De schakelaar erin hangen. |
| `src/components/dashboard/dashboard-chart.tsx` | Balk, astekst en tooltip op tokens. |
| `src/components/vacation/absence-client.tsx` | 32 kleurplekken. |
| `src/components/ui/badge.tsx` | 2 varianten. |
| `src/components/hours-overview/hours-overview-client.tsx` | 4 plekken. |
| `src/app/(app)/page.tsx` | 3 plekken. |
| `src/components/personeel/reviews-admin-client.tsx` | 1 paneel. |
| `src/components/personeel/contracts-client.tsx` | 1 paneel. |
| `src/components/settings/settings-client.tsx` | 1 plek. |
| `src/components/reviews/template-editor-client.tsx` | 1 plek. |
| `src/components/reports/reports-client.tsx` | 1 plek. |

---

## Task 1: De thema-functie

**Files:**
- Create: `src/lib/theme.ts`
- Test: `src/lib/theme.test.ts`

**Interfaces:**
- Produces: `THEME_STORAGE_KEY: string` (`"theme"`), `type ThemeChoice = "light" | "dark" | "system"`, `type AppliedTheme = "light" | "dark"`, en `resolveTheme(stored: string | null, prefersDark: boolean): AppliedTheme`. Task 3 gebruikt alle vier; Task 2 schrijft dezelfde regel met de hand uit in het inline script.

- [ ] **Step 1: Schrijf de falende test**

Maak `src/lib/theme.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { resolveTheme, THEME_STORAGE_KEY } from "./theme";

describe("THEME_STORAGE_KEY", () => {
  it("is the key the inline script in layout.tsx also reads", () => {
    expect(THEME_STORAGE_KEY).toBe("theme");
  });
});

describe("resolveTheme", () => {
  it("honours an explicit light choice even when the system is dark", () => {
    expect(resolveTheme("light", true)).toBe("light");
  });

  it("honours an explicit dark choice even when the system is light", () => {
    expect(resolveTheme("dark", false)).toBe("dark");
  });

  it("follows a dark system when the choice is system", () => {
    expect(resolveTheme("system", true)).toBe("dark");
  });

  it("follows a light system when the choice is system", () => {
    expect(resolveTheme("system", false)).toBe("light");
  });

  it("follows the system when nothing is stored yet", () => {
    expect(resolveTheme(null, true)).toBe("dark");
    expect(resolveTheme(null, false)).toBe("light");
  });

  it("follows the system on a value it does not recognise", () => {
    // localStorage is door de gebruiker te bewerken en overleeft een
    // hernoeming van de waarden. Een onbekende waarde mag nooit een leeg of
    // half thema opleveren; hij valt terug op wat het systeem zegt.
    expect(resolveTheme("blauw", true)).toBe("dark");
    expect(resolveTheme("", false)).toBe("light");
  });
});
```

- [ ] **Step 2: Draai de test en stel vast dat hij faalt**

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npx vitest run src/lib/theme.test.ts`
Expected: FAIL — de module `./theme` bestaat niet.

- [ ] **Step 3: Schrijf de implementatie**

Maak `src/lib/theme.ts`:

```ts
/**
 * Welk thema er getoond wordt.
 *
 * De gebruiker kiest uit drie standen; er zijn er maar twee om te tonen.
 * `resolveTheme` is de vertaling daartussen, en die staat hier apart omdat
 * twee plekken hem nodig hebben: de schakelaar in de sidebar, en het inline
 * script in de layout dat draait voordat er ook maar iets geladen is.
 */

/** De sleutel in localStorage. Het inline script leest dezelfde. */
export const THEME_STORAGE_KEY = "theme";

/** Wat de gebruiker kan kiezen. */
export type ThemeChoice = "light" | "dark" | "system";

/** Wat er uiteindelijk op het scherm staat. */
export type AppliedTheme = "light" | "dark";

/**
 * De opgeslagen keuze plus de systeemvoorkeur wordt één toegepast thema.
 *
 * Alles wat geen expliciete keuze is — `"system"`, niets opgeslagen, of een
 * waarde die hier niet thuishoort — volgt het systeem. Dat laatste is opzet:
 * localStorage is door de gebruiker te bewerken, en een onbekende waarde mag
 * nooit tot een leeg of half thema leiden.
 */
export function resolveTheme(stored: string | null, prefersDark: boolean): AppliedTheme {
  if (stored === "light" || stored === "dark") return stored;
  return prefersDark ? "dark" : "light";
}
```

- [ ] **Step 4: Draai de test en stel vast dat hij slaagt**

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npx vitest run src/lib/theme.test.ts`
Expected: PASS, 7 tests.

- [ ] **Step 5: Draai de volledige suite**

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npm test`
Expected: 24 bestanden, 255 tests, groen.

- [ ] **Step 6: Commit**

```bash
git add src/lib/theme.ts src/lib/theme.test.ts
git commit -m "feat: thema-keuze omzetten naar een toegepast thema"
```

---

## Task 2: Het mechanisme — tokens en geen flits

**Files:**
- Modify: `src/app/globals.css`
- Modify: `src/app/layout.tsx`

**Interfaces:**
- Consumes: de sleutel `"theme"` en de regel uit `resolveTheme` (Task 1), hier met de hand uitgeschreven omdat een inline script niet kan importeren.
- Produces: de klasse `dark` op `<html>`, waar Task 3 op verder bouwt.

**Deze taak levert geen unittests op.** Het is CSS plus een script dat vóór React draait; deze repo test uitsluitend pure functies.

- [ ] **Step 1: Zet de dark-variant om naar de klasse**

In `src/app/globals.css` staat als eerste regel:

```css
@import "tailwindcss";
```

Maak daarvan:

```css
@import "tailwindcss";

/* Tailwind 4 laat `dark:` standaard naar prefers-color-scheme kijken. Wij
   sturen het thema met een klasse op <html>, omdat de gebruiker het expliciet
   moet kunnen kiezen. Zonder deze regel doet elke dark:-utility het
   verkeerde: hij volgt dan het besturingssysteem in plaats van de keuze. */
@custom-variant dark (&:where(.dark, .dark *));
```

- [ ] **Step 2: Voeg de donkere tokens toe**

In datzelfde bestand eindigt het `:root`-blok met `--radius: 0.5rem;` en een `}`. Voeg **direct daarna** toe, vóór `@theme inline`:

```css
/* Dezelfde negentien kleurtokens als hierboven, in het donker. --radius hoort
   niet bij het thema en staat daarom alleen in :root. */
.dark {
  --background: 222 47% 7%;
  --foreground: 210 40% 98%;
  /* Lichter dan --background, waar de lichte variant ze allebei op wit heeft:
     een kaart die exact de paginakleur heeft, verdwijnt in het donker. */
  --card: 222 40% 11%;
  --card-foreground: 210 40% 98%;
  --popover: 222 40% 11%;
  --popover-foreground: 210 40% 98%;
  /* Zelfde tint als het EVAbits-groen in :root, maar lichter: tegen wit
     gekozen, en tegen bijna-zwart zakt 36% weg. */
  --primary: 121 40% 55%;
  --primary-foreground: 222 47% 9%;
  --secondary: 217 33% 18%;
  --secondary-foreground: 210 40% 98%;
  --muted: 217 33% 18%;
  --muted-foreground: 215 20% 65%;
  --accent: 217 33% 18%;
  --accent-foreground: 210 40% 98%;
  /* Minder verzadigd dan in :root, want fel rood op bijna-zwart trilt. De
     helderheid blijft vrijwel gelijk: deze token staat hier vooral op
     foutmeldingen, en dan telt leesbaarheid. */
  --destructive: 0 72% 58%;
  --destructive-foreground: 210 40% 98%;
  --border: 217 33% 20%;
  --input: 217 33% 20%;
  --ring: 121 40% 55%;
}
```

- [ ] **Step 3: Lees hoe deze Next.js met een script in de layout omgaat**

Voordat je `layout.tsx` aanraakt: zoek in `node_modules/next/dist/docs/` op waar een blokkerend `<script>` in de App Router hoort en of een handgeschreven `<head>` in een root layout is toegestaan. Deze Next.js-versie wijkt af van wat je gewend bent. Noteer in je rapport wat je gevonden hebt en waar.

- [ ] **Step 4: Zet de klasse vóór het schilderen**

`src/app/layout.tsx` is nu:

```tsx
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="nl" className={`${geistSans.variable} ${geistMono.variable} h-full`}>
      <body className="h-full antialiased">{children}</body>
    </html>
  );
}
```

Voeg boven die functie toe:

```tsx
/**
 * Zet de thema-klasse voordat de browser iets schildert.
 *
 * Elke pagina is server-gerenderd. Zonder dit script schildert de browser
 * eerst het lichte thema en pas na het laden van React het donkere — een
 * witte flits bij élke navigatie. Daarom inline en blokkerend.
 *
 * Dit is dezelfde regel als resolveTheme in src/lib/theme.ts, met de hand
 * uitgeschreven: een script dat vóór de bundel draait kan niet importeren.
 * Wijzigt die functie, wijzig hier dan mee — de test op THEME_STORAGE_KEY
 * bewaakt alleen de sleutel, niet deze regel.
 */
const themaScript = `
try {
  var keuze = localStorage.getItem("theme");
  var donker = keuze === "dark"
    || (keuze !== "light" && matchMedia("(prefers-color-scheme: dark)").matches);
  if (donker) document.documentElement.classList.add("dark");
} catch (e) {}
`;
```

En vervang de component door:

```tsx
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    // suppressHydrationWarning omdat het script hierboven de class van <html>
    // wijzigt vóórdat React hydrateert; de server-HTML wijkt dan per definitie af.
    <html
      lang="nl"
      className={`${geistSans.variable} ${geistMono.variable} h-full`}
      suppressHydrationWarning
    >
      <head>
        <script dangerouslySetInnerHTML={{ __html: themaScript }} />
      </head>
      <body className="h-full antialiased">{children}</body>
    </html>
  );
}
```

Wijkt dit af van wat je in Step 3 in de Next.js-documentatie hebt gevonden, volg dan de documentatie en meld het verschil in je rapport.

- [ ] **Step 5: Controleer de logica tegen `resolveTheme`**

Loop de vier gevallen na en stel vast dat het script hetzelfde doet als `resolveTheme` uit Task 1:

| `localStorage.theme` | systeem donker | script | `resolveTheme` |
|---|---|---|---|
| `"dark"` | nee | donker | donker |
| `"light"` | ja | licht | licht |
| `"system"` | ja | donker | donker |
| niets of onzin | ja | donker | donker |

Meld in je rapport dat je dit hebt nagelopen. Wijkt er één af, dan is het script fout en niet de tabel.

- [ ] **Step 6: Controleer de typen en de tests**

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npx tsc --noEmit`
Expected: 0 fouten.

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npm test`
Expected: 24 bestanden, 255 tests, groen.

- [ ] **Step 7: Commit**

```bash
git add src/app/globals.css src/app/layout.tsx
git commit -m "feat: donkere kleurtokens en de klasse vóór het schilderen"
```

---

## Task 3: De schakelaar

**Files:**
- Create: `src/components/layout/theme-toggle.tsx`
- Modify: `src/components/layout/sidebar.tsx`

**Interfaces:**
- Consumes: `resolveTheme`, `THEME_STORAGE_KEY` en `type ThemeChoice` uit Task 1; de klasse `dark` uit Task 2.
- Produces: `<ThemeToggle />`, een component zonder props.

**Deze taak levert geen unittests op.** De logica zit in Task 1; wat hier bijkomt is componenttoestand, en die testvorm bestaat niet in deze repo.

- [ ] **Step 1: Schrijf de schakelaar**

Maak `src/components/layout/theme-toggle.tsx`:

```tsx
"use client";
import { useEffect, useState } from "react";
import { Sun, Moon, Monitor } from "lucide-react";
import { resolveTheme, THEME_STORAGE_KEY, type ThemeChoice } from "@/lib/theme";

const KEUZES: Array<{ waarde: ThemeChoice; label: string; Icon: typeof Sun }> = [
  { waarde: "light", label: "Licht", Icon: Sun },
  { waarde: "dark", label: "Donker", Icon: Moon },
  { waarde: "system", label: "Systeem", Icon: Monitor },
];

export function ThemeToggle() {
  // null betekent "nog niet uit localStorage gelezen". Op de server bestaat
  // localStorage niet, dus lezen kan pas na het hydrateren; tot die tijd mag
  // dit component de klasse niet aanraken, want het inline script in de
  // layout heeft hem dan al goed gezet.
  const [keuze, setKeuze] = useState<ThemeChoice | null>(null);

  useEffect(() => {
    const opgeslagen = localStorage.getItem(THEME_STORAGE_KEY);
    setKeuze(opgeslagen === "light" || opgeslagen === "dark" ? opgeslagen : "system");
  }, []);

  useEffect(() => {
    if (keuze === null) return;
    const media = window.matchMedia("(prefers-color-scheme: dark)");
    const pas = () => {
      const thema = resolveTheme(keuze, media.matches);
      document.documentElement.classList.toggle("dark", thema === "dark");
    };
    pas();
    // Alleen meebewegen met het besturingssysteem als de gebruiker daar zelf
    // voor gekozen heeft; anders overrulet het systeem zijn keuze.
    if (keuze !== "system") return;
    media.addEventListener("change", pas);
    return () => media.removeEventListener("change", pas);
  }, [keuze]);

  function kies(waarde: ThemeChoice) {
    localStorage.setItem(THEME_STORAGE_KEY, waarde);
    setKeuze(waarde);
  }

  return (
    <div className="flex gap-1 rounded-md border p-1" role="group" aria-label="Thema">
      {KEUZES.map(({ waarde, label, Icon }) => (
        <button
          key={waarde}
          type="button"
          onClick={() => kies(waarde)}
          aria-pressed={keuze === waarde}
          title={label}
          className={`flex flex-1 items-center justify-center rounded-sm p-1.5 transition-colors ${
            keuze === waarde
              ? "bg-accent text-accent-foreground"
              : "text-muted-foreground hover:bg-accent/50"
          }`}
        >
          <Icon className="h-4 w-4" />
          <span className="sr-only">{label}</span>
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: Hang hem in de sidebar**

In `src/components/layout/sidebar.tsx` staat onderin:

```tsx
        <div className="p-4 border-t">
          <div className="mb-2 px-3">
            <p className="text-sm font-medium truncate">{user.name}</p>
            <p className="text-xs text-muted-foreground truncate">{user.email}</p>
          </div>
          <Button
```

Voeg de import toe bij de andere imports bovenaan het bestand:

```tsx
import { ThemeToggle } from "@/components/layout/theme-toggle";
```

En zet de schakelaar tussen het naamblok en de uitlogknop:

```tsx
        <div className="p-4 border-t">
          <div className="mb-2 px-3">
            <p className="text-sm font-medium truncate">{user.name}</p>
            <p className="text-xs text-muted-foreground truncate">{user.email}</p>
          </div>
          <div className="mb-2 px-3">
            <ThemeToggle />
          </div>
          <Button
```

- [ ] **Step 3: Controleer de typen en de tests**

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npx tsc --noEmit`
Expected: 0 fouten.

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npm test`
Expected: 24 bestanden, 255 tests, groen.

- [ ] **Step 4: Commit**

```bash
git add src/components/layout/theme-toggle.tsx src/components/layout/sidebar.tsx
git commit -m "feat: thema-schakelaar in de sidebar"
```

---

## Task 4: De vaste kleuren

**Files:**
- Modify: `src/components/vacation/absence-client.tsx`
- Modify: `src/components/ui/badge.tsx`
- Modify: `src/components/hours-overview/hours-overview-client.tsx`
- Modify: `src/app/(app)/page.tsx`
- Modify: `src/components/personeel/reviews-admin-client.tsx`
- Modify: `src/components/personeel/contracts-client.tsx`
- Modify: `src/components/settings/settings-client.tsx`
- Modify: `src/components/reviews/template-editor-client.tsx`
- Modify: `src/components/reports/reports-client.tsx`

**Interfaces:**
- Consumes: de `dark:`-variant uit Task 2. Zonder die `@custom-variant`-regel doet elke toevoeging hier het verkeerde, dus Task 2 moet af zijn.

**Deze taak levert geen unittests op.** Het zijn klassenamen.

**Het recept.** Voeg toe, verwijder niets:

| Patroon | Erbij |
|---|---|
| `bg-X-100 text-X-800` | `dark:bg-X-900/40 dark:text-X-200` |
| `hover:bg-X-100` | `dark:hover:bg-X-900/40` |
| `text-X-600` of `text-X-700` | `dark:text-X-400` |
| `bg-X-50` of `bg-X-500/5` | `dark:bg-X-950/30` |
| `border-X-200` of `border-X-500/40` | `dark:border-X-900` |

- [ ] **Step 1: `absence-client.tsx` — de badges**

Regels 119 tot en met 121:

```tsx
  if (status === "APPROVED") return <Badge className="bg-green-100 text-green-800 hover:bg-green-100">Goedgekeurd</Badge>;
  if (status === "REJECTED") return <Badge className="bg-red-100 text-red-800 hover:bg-red-100">Afgewezen</Badge>;
  return <Badge className="bg-yellow-100 text-yellow-800 hover:bg-yellow-100">In afwachting</Badge>;
```

wordt:

```tsx
  if (status === "APPROVED") return <Badge className="bg-green-100 text-green-800 hover:bg-green-100 dark:bg-green-900/40 dark:text-green-200 dark:hover:bg-green-900/40">Goedgekeurd</Badge>;
  if (status === "REJECTED") return <Badge className="bg-red-100 text-red-800 hover:bg-red-100 dark:bg-red-900/40 dark:text-red-200 dark:hover:bg-red-900/40">Afgewezen</Badge>;
  return <Badge className="bg-yellow-100 text-yellow-800 hover:bg-yellow-100 dark:bg-yellow-900/40 dark:text-yellow-200 dark:hover:bg-yellow-900/40">In afwachting</Badge>;
```

- [ ] **Step 2: `absence-client.tsx` — de vijf verlofchips**

Regels 127 tot en met 131:

```tsx
    VACATION: "bg-blue-100 text-blue-800 hover:bg-blue-100",
    SICK: "bg-orange-100 text-orange-800 hover:bg-orange-100",
    PARENTAL_LEAVE: "bg-pink-100 text-pink-800 hover:bg-pink-100",
    SPECIAL_LEAVE: "bg-purple-100 text-purple-800 hover:bg-purple-100",
    UNPAID_LEAVE: "bg-gray-100 text-gray-800 hover:bg-gray-100",
```

wordt:

```tsx
    VACATION: "bg-blue-100 text-blue-800 hover:bg-blue-100 dark:bg-blue-900/40 dark:text-blue-200 dark:hover:bg-blue-900/40",
    SICK: "bg-orange-100 text-orange-800 hover:bg-orange-100 dark:bg-orange-900/40 dark:text-orange-200 dark:hover:bg-orange-900/40",
    PARENTAL_LEAVE: "bg-pink-100 text-pink-800 hover:bg-pink-100 dark:bg-pink-900/40 dark:text-pink-200 dark:hover:bg-pink-900/40",
    SPECIAL_LEAVE: "bg-purple-100 text-purple-800 hover:bg-purple-100 dark:bg-purple-900/40 dark:text-purple-200 dark:hover:bg-purple-900/40",
    UNPAID_LEAVE: "bg-gray-100 text-gray-800 hover:bg-gray-100 dark:bg-gray-800/60 dark:text-gray-200 dark:hover:bg-gray-800/60",
```

`UNPAID_LEAVE` wijkt af van het recept: grijs op `900/40` is in het donker niet van de achtergrond te onderscheiden, dus die krijgt `800/60`.

- [ ] **Step 3: `absence-client.tsx` — de statuskleuren**

Er staan zeven plekken met `text-red-600` of `text-green-600` — vijf rode en twee groene. Voeg bij élk `text-red-600` het woord `dark:text-red-400` toe en bij élk `text-green-600` het woord `dark:text-green-400`. De regels:

- 398: `${myRemaining < 0 ? "text-red-600" : ""}` wordt `${myRemaining < 0 ? "text-red-600 dark:text-red-400" : ""}`
- 490: `${remaining < 0 ? "text-red-600" : ""}` wordt `${remaining < 0 ? "text-red-600 dark:text-red-400" : ""}`
- 541: `<Check className="h-4 w-4 text-green-600" />` wordt `<Check className="h-4 w-4 text-green-600 dark:text-green-400" />`
- 623: `"text-red-600 font-medium"` wordt `"text-red-600 dark:text-red-400 font-medium"`
- 819: `"text-red-600"` wordt `"text-red-600 dark:text-red-400"`
- 834: `className="h-7 w-7 text-green-600"` wordt `className="h-7 w-7 text-green-600 dark:text-green-400"`
- 843: `className="h-7 w-7 text-red-600"` wordt `className="h-7 w-7 text-red-600 dark:text-red-400"`

Regel 425 (`bg-yellow-500 text-white`) blijft ongewijzigd: dat is een verzadigde bol met witte tekst die in beide thema's hetzelfde leest.

- [ ] **Step 4: `badge.tsx`**

Regels 14 en 15:

```tsx
        success: "border-transparent bg-green-100 text-green-800",
        warning: "border-transparent bg-yellow-100 text-yellow-800",
```

wordt:

```tsx
        success: "border-transparent bg-green-100 text-green-800 dark:bg-green-900/40 dark:text-green-200",
        warning: "border-transparent bg-yellow-100 text-yellow-800 dark:bg-yellow-900/40 dark:text-yellow-200",
```

- [ ] **Step 5: `hours-overview-client.tsx`**

Regels 54 tot en met 56:

```tsx
  if (delta >= 0) return "text-green-600";
  if (delta >= -10) return "text-amber-600";
  return "text-red-600";
```

wordt:

```tsx
  if (delta >= 0) return "text-green-600 dark:text-green-400";
  if (delta >= -10) return "text-amber-600 dark:text-amber-400";
  return "text-red-600 dark:text-red-400";
```

Regel 197:

```tsx
                          <span className="flex items-center gap-1 text-xs text-green-600 justify-end">
```

wordt:

```tsx
                          <span className="flex items-center gap-1 text-xs text-green-600 dark:text-green-400 justify-end">
```

- [ ] **Step 6: `src/app/(app)/page.tsx`**

Regel 154:

```tsx
          <Card className="border-amber-500/40 bg-amber-500/5">
```

wordt:

```tsx
          <Card className="border-amber-500/40 bg-amber-500/5 dark:border-amber-900 dark:bg-amber-950/30">
```

Regel 156:

```tsx
              <FolderOpen className="h-5 w-5 text-amber-600" />
```

wordt:

```tsx
              <FolderOpen className="h-5 w-5 text-amber-600 dark:text-amber-400" />
```

Regel 224:

```tsx
            <div className={`text-2xl font-bold ${vacRemainingHours < 0 ? "text-red-600" : ""}`}>
```

wordt:

```tsx
            <div className={`text-2xl font-bold ${vacRemainingHours < 0 ? "text-red-600 dark:text-red-400" : ""}`}>
```

- [ ] **Step 7: `reviews-admin-client.tsx`**

Regel 198:

```tsx
        <p className="text-sm text-green-700 bg-green-50 border border-green-200 rounded px-3 py-2">
```

wordt:

```tsx
        <p className="text-sm text-green-700 bg-green-50 border border-green-200 rounded px-3 py-2 dark:text-green-400 dark:bg-green-950/30 dark:border-green-900">
```

- [ ] **Step 8: `contracts-client.tsx`**

Regel 311:

```tsx
              <p className="text-sm text-amber-600 bg-amber-50 border border-amber-200 rounded px-3 py-2">
```

wordt:

```tsx
              <p className="text-sm text-amber-600 bg-amber-50 border border-amber-200 rounded px-3 py-2 dark:text-amber-400 dark:bg-amber-950/30 dark:border-amber-900">
```

- [ ] **Step 9: De laatste drie**

`settings-client.tsx` regel 203:

```tsx
              {saved && <p className="text-sm text-green-600">Opgeslagen!</p>}
```

wordt:

```tsx
              {saved && <p className="text-sm text-green-600 dark:text-green-400">Opgeslagen!</p>}
```

`template-editor-client.tsx` regel 183:

```tsx
          <p className={`text-sm ${status.ok ? "text-green-600" : "text-destructive"}`}>{status.msg}</p>
```

wordt:

```tsx
          <p className={`text-sm ${status.ok ? "text-green-600 dark:text-green-400" : "text-destructive"}`}>{status.msg}</p>
```

`reports-client.tsx` regel 216:

```tsx
                              ? <span className="text-amber-600 font-medium">+{formatHours(extraHours)}</span>
```

wordt:

```tsx
                              ? <span className="text-amber-600 dark:text-amber-400 font-medium">+{formatHours(extraHours)}</span>
```

- [ ] **Step 10: Controleer dat er niets is overgeslagen**

Run:

```bash
grep -rnE "(text|bg|border)-(red|green|blue|amber|yellow|orange|gray|purple|pink)-[0-9]{2,3}" src | grep -v "dark:" | grep -v "bg-yellow-500"
```

Expected: geen uitvoer. Elke regel met een vaste kleur heeft nu ook een `dark:`-tegenhanger, op de bewust ongemoeide `bg-yellow-500` na. Komt er wél iets terug, werk dat af volgens het recept bovenaan deze taak en meld het in je rapport.

- [ ] **Step 11: Controleer de typen, de tests en de lint**

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npx tsc --noEmit`
Expected: 0 fouten.

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npm test`
Expected: 24 bestanden, 255 tests, groen.

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npm run lint`
Expected: 323 errors en 20 warnings, gelijk aan de baseline.

- [ ] **Step 12: Commit**

```bash
git add src/components/vacation/absence-client.tsx src/components/ui/badge.tsx src/components/hours-overview/hours-overview-client.tsx "src/app/(app)/page.tsx" src/components/personeel/reviews-admin-client.tsx src/components/personeel/contracts-client.tsx src/components/settings/settings-client.tsx src/components/reviews/template-editor-client.tsx src/components/reports/reports-client.tsx
git commit -m "feat: vaste kleuren krijgen een donkere tegenhanger"
```

---

## Task 5: De grafiek

**Files:**
- Modify: `src/components/dashboard/dashboard-chart.tsx`

**Interfaces:**
- Consumes: de tokens uit Task 2.

**Deze taak levert geen unittests op.**

- [ ] **Step 1: Zet de drie kleuren op tokens**

Het hele blok van `<BarChart>` tot en met `</BarChart>`:

```tsx
            <BarChart data={data} margin={{ top: 0, right: 0, left: -20, bottom: 0 }}>
              <XAxis dataKey="name" tick={{ fontSize: 11 }} />
              <YAxis tick={{ fontSize: 11 }} />
              <Tooltip
                formatter={(value) => [`${Number(value).toFixed(2)} uur`, "Uren"]}
                labelFormatter={(label) => `Project: ${label}`}
              />
              <Bar dataKey="hours" fill="hsl(121 37% 36%)" radius={[4, 4, 0, 0]} />
            </BarChart>
```

wordt:

```tsx
            <BarChart data={data} margin={{ top: 0, right: 0, left: -20, bottom: 0 }}>
              {/* Recharts tekent met inline SVG-attributen en kent geen
                  Tailwind-klassen, dus de tokens gaan hier met de hand mee.
                  Ze zijn niet hardgecodeerd maar verwijzen naar dezelfde
                  variabelen als de rest van de app, zodat de grafiek meekantelt. */}
              <XAxis dataKey="name" tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} />
              <YAxis tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} />
              <Tooltip
                formatter={(value) => [`${Number(value).toFixed(2)} uur`, "Uren"]}
                labelFormatter={(label) => `Project: ${label}`}
                contentStyle={{
                  background: "hsl(var(--popover))",
                  border: "1px solid hsl(var(--border))",
                  borderRadius: "var(--radius)",
                  color: "hsl(var(--popover-foreground))",
                }}
                labelStyle={{ color: "hsl(var(--popover-foreground))" }}
                itemStyle={{ color: "hsl(var(--popover-foreground))" }}
              />
              <Bar dataKey="hours" fill="hsl(var(--primary))" radius={[4, 4, 0, 0]} />
            </BarChart>
```

De balk stond op `hsl(121 37% 36%)`, een tweede kopie van het EVAbits-groen uit `globals.css`. Via de token beweegt hij niet alleen mee met het thema, maar ook als dat groen ooit verandert.

- [ ] **Step 2: Controleer de typen en de tests**

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npx tsc --noEmit`
Expected: 0 fouten.

Run: `PATH="$HOME/.nvm/versions/node/v20.20.1/bin:$PATH" npm test`
Expected: 24 bestanden, 255 tests, groen.

- [ ] **Step 3: Commit**

```bash
git add src/components/dashboard/dashboard-chart.tsx
git commit -m "feat: grafiek gebruikt de kleurtokens in plaats van vaste waarden"
```

---

## Uitrol

Geen migratie, geen backfill, geen omgevingsvariabelen. Pushen naar `main` volstaat en Vercel deployt.

Handmatig na te lopen na de deploy:

- [ ] Schakelen tussen Licht, Donker en Systeem verandert het scherm direct, zonder herladen.
- [ ] Herladen op donker geeft **geen witte flits**. Dit is de belangrijkste: hier is het inline script voor.
- [ ] Doorklikken naar een andere pagina geeft ook geen flits.
- [ ] Op Systeem: het thema van je besturingssysteem omzetten kantelt de app mee terwijl hij open staat.
- [ ] Op Licht of Donker: hetzelfde doen verandert er níets aan.
- [ ] `localStorage.setItem("theme", "blauw")` en herladen → de app volgt het systeem in plaats van kapot te gaan.
- [ ] De verlofpagina in het donker: de vijf verlofsoort-chips en de statusbadges zijn leesbaar.
- [ ] Het dashboard in het donker: de kaart "projecten zonder klant" is een donker paneel, geen wit vlak, en de grafiek heeft leesbare astekst met een donkere tooltip.
- [ ] Een factuur-e-mail versturen → die is nog steeds licht; het thema van de verzender hoort daar niet in door te lekken.
- [ ] Een factuur-PDF openen → ongewijzigd.
